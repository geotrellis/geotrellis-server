http {
    "interface": "0.0.0.0"
    "interface": ${?HTTP_INTERFACE}
    "port": 9000
    "port": ${?HTTP_PORT}
}

auth {
    "signing-key": ${?SIGNING_KEY}
    "signing-key": "REPLACEME"
}

catalog {
    "uri": "s3://azavea-datahub/catalog"
}
